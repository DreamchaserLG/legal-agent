# 加拿大法律 Agent 优化计划

## 实施状态（2026-09-10）

- P0：进行中。RAD 分区 NUL 字符导致全量导入停止，现已在公共文本清洗层修复并恢复；未达到 225807 条前不执行全量同步和向量重建。
- P1：最小可用版本已实现。提供 7 个本地技能、只读检索、正式关联读取、证据清单、注入拦截和 `skill_runs` 审计；未安装或执行第三方技能仓库。
- P2：未启动训练。缺少可合法使用、人工复核、按时间切分的风险标签数据，当前只提供风险复核草稿。
- P3：未启动。没有接入外部 API、商业服务或 MCP，也未发送案件数据到外部平台。

## 1. 当前基线与结论

记录时间：2026-09-09（数据库读取时点）

- A2AJ 原始案例：`168827` 条，正文非空 `168827` 条；全量目标为 `225807` 条。
- 已正式同步到 `legal_cases`：`1018` 条。全量导入尚未结束，后续的结构化同步、案例-法规关联、RAG 重建和 BGE-M3 向量化尚未对新增 A2AJ 数据启动。
- 现有正式案例中，至少有一条法规关联的案例为 `544/1018`，关联覆盖率为 `53.44%`。这是当前真实覆盖率，不应以历史小样本检索命中率替代。
- 当前 `rag_chunk_embeddings` 为 `229631`，其中 BGE-M3 1024 维向量为 `26285`，维度有效 `26285`。全量语义向量迁移尚未完成。
- 结论：优先级必须是“完成可追溯的数据闭环和评测基线”，不能在原始数据尚未同步时把外部技能包直接接入生产问答链路。

## 2. 外部资料审查

### 2.1 可借鉴的开源项目

| 资料 | 可复用价值 | 不直接复用的原因 | 处置 |
| --- | --- | --- | --- |
| CaLegal | 加拿大优先的任务包、技能路由、治理审计和结果包思想 | 需要逐文件审查许可证、依赖、数据处理和提示词；不能把未审查运行时直接嵌入现有服务 | P1：以“工作包 + 审计事件”设计为参考，先实现本地兼容层 |
| Legal-Skills-Chinese | 原子技能到复合技能的分层、要件提取、论证链、风险优先级、人工复核边界 | 规则面向中国大陆成文法，不能把中国法实体规则、引用方法或裁判预测逻辑用于加拿大普通法和魁北克民法 | P1：只提取通用工作流结构，重写加拿大法域技能内容 |
| Legal AI Skills / gcheng001 legal-skills | 文书处理、事实核查、流程型技能的编排思路 | 仍以中国法为主要语境，且必须核对许可证、维护状态和依赖 | P2：仅作为测试用例与交互流程参考 |
| @gonzih/skills-legal | 合同扫描、研究摘要、客户接洽、和解摘要等通用输出模板 | 默认业务与法域并非加拿大；输出必须受证据引用和人工复核约束 | P2：选择性重写为加拿大输出模板 |

### 2.2 商业或受限服务

- Westlaw、Lexis+、vLex、ThAImis、Apogee、Clio Work：仅在获得有效合同、公开或书面 API 权限、数据处理协议和费用批准后评估适配器；不得模拟浏览器、规避登录或抓取受限数据。
- CanLII Search+：适合作为人工研究的外部对照工具，不作为自动批量数据源或生产工具调用接口。官方资料说明其基于 CanLII 自身语料、受每日使用限制，并定位为研究辅助而不是问答机器人。
- OpenJustice：可关注其无代码 Agent 与评测工作流，但在确认 API、数据驻留、隐私条款和商业许可前不传输案件材料。

## 3. 目标架构

### 3.1 本地技能运行时

新增 `app/skills/`，采用声明式 `SKILL.md` 和 YAML/JSON 清单，但运行时只调用本项目受控服务：

1. `case_retrieval_ca`：先用结构化过滤缩小法域、法院、日期和文书类型，再用 BGE-M3 + HNSW + GIN/RRF 检索。
2. `statute_retrieval_ca`：检索现行法条、版本日期、管辖区和条款级证据。
3. `issue_fact_extraction`：从用户输入抽取争点、事实、时间、地点、法院、当事人角色和缺失事实，不下法律结论。
4. `authority_linking`：显示案例到法规、被引案例与反向引用，给每条关系保留来源、抽取方法、置信度和可复核证据。
5. `evidence_verification`：逐项验证回答中的每一个法律主张都能定位到已检索的原文片段；不能支持的主张必须删除或标为“不足以判断”。
6. `risk_assessment_ca`：输出风险因素、相反因素、事实缺口、适用前提、置信区间和人工复核标记，不输出确定性胜诉承诺。
7. `research_memo_ca`：生成带段落定位、案例引用、法规版本和时间戳的研究备忘录草稿。

### 3.2 审计与治理

- 每次技能调用写入 `agent_runs`：输入摘要、技能版本、检索过滤条件、候选文档 ID、重排分数、模型版本、耗时和最终证据 ID。
- 每个结论建立“主张 -> 原文片段 -> 文档 -> 来源版本”的可追溯链；禁止只保留模型生成文本。
- 涉及诉讼策略、判决预测、劳动雇佣或人权风险的输出强制标记为“律师复核草稿”。
- 上传文件、外部链接和 MCP 返回内容先经过提示注入检测、内容长度限制和来源信任等级分类。

## 4. 分阶段实施顺序

### P0：完成数据闭环与质量门禁

前置条件：A2AJ `225807` 条案例完整入库。

1. 执行现有全量流水线的结构化同步，确保 `source_items -> legal_cases` 一对一映射完整。
2. 重建案例-法规关系；分别统计覆盖率、每案平均关系数、孤立案例率和无效法规 ID。
3. 按 512 token / 64 token overlap 重建新增案例分块，迁移为 BGE-M3 1024 维归一化向量。
4. 执行数据质量门禁：空正文、重复 UID、缺失外键、向量维度、内容哈希、单位范数、HNSW 查询计划和 P95 延迟。
5. 产出冻结的评测集：覆盖联邦、安大略、魁北克、BC、劳动、合同、刑事、人权和行政法；每题由人工标注应命中案例、法规、段落和不可回答条件。

验收标准：全量导入完成；新增源的同步率 `100%`；空正文 `0`；向量异常 `0`；案例-法规覆盖率与人工评测结果分别记录，不混淆“覆盖率”和“检索准确率”。

### P1：实现加拿大法域技能包

1. 实现上述 7 个本地技能，先只调用本地 PostgreSQL、pgvector 和现有 Qwen 服务。
2. 为每个技能增加输入输出 JSON Schema、超时、幂等键和失败降级策略。
3. 使用“检索、重排、证据校验、生成”四阶段流水线；生成模型不能自行补充未检索到的判例或法条。
4. 提供英语、法语输入支持；中文仅作为界面解释语言，不将中文译文视为加拿大法源。

验收标准：每条法律结论的证据覆盖率可计算；无证据主张率、错误引用率、拒答正确率和人工复核通过率均进入回归测试。

### P2：风险评估与预测闭环

1. 将“风险评估”拆为事实完整度、法源匹配、先例相似度、程序状态、时效、证据质量和不确定性，避免把单一模型分数包装为胜诉概率。
2. 建立人工标注队列，数据结构保留标注人角色、来源、标注指南版本、法域、案件时间和置信度。
3. 只有在拥有足够、合法且时间切分正确的标注数据后，训练校准模型；使用时间外测试集、Brier score、校准曲线、分法域指标和拒答阈值评估。
4. 模型输出只用于辅助排序和风险提示；对高影响结果强制展示依据和人工复核状态。

验收标准：不以“类人”作为目标。以可解释、可校准、可复现和人工复核通过率为硬指标；未达到阈值时仅提供检索与分析，不提供概率预测。

### P3：外部技能和 MCP 适配

1. 对候选仓库逐项完成许可证、提交历史、依赖漏洞、网络行为、提示注入、数据外传和法域适配审查。
2. 仅将审核通过的技能翻译为本地清单，不执行第三方安装脚本，不授予数据库写权限。
3. MCP 适配器必须是最小权限：默认只读、请求级白名单、审计日志、速率限制、敏感字段脱敏和可开关配置。
4. 商业平台只通过正式 API 和用户授权账户接入；没有书面授权则保留人工导出/人工复核流程。

验收标准：每个外部工具可独立禁用；任何失败不会阻塞本地检索；不会将 API 密钥、案件正文或个人信息写入 Git、日志或第三方技能目录。

## 5. 优先级与不采用项

- 立即优先：P0、加拿大本地技能运行时、证据核验和评测集。
- 后续优先：CaLegal 的治理与审计思想、通用合同/备忘录工作流。
- 不直接采用：面向中国实体法的技能内容、无公开 API 的商业平台自动化、CanLII 批量抓取、任何要求绕过登录、配额或反爬措施的实现。
- 需要用户批准后才可开始：下载或安装外部仓库、接入第三方 API/MCP、发送案件材料到外部服务、商业订阅和任何模型训练任务。

## 6. 验收面板

- 数据：A2AJ 导入总数、正文非空率、同步率、重复率、案例-法规关联覆盖率。
- 检索：case recall@k、statute recall@k、段落级 nDCG、证据可定位率、P50/P95 延迟。
- 生成：引用真实性、无证据主张率、拒答正确率、人工复核通过率。
- 风险：校准误差、Brier score、分法域偏差、升级人工复核比例。
- 运维：任务失败恢复时间、断点续传成功率、外部适配器错误率、审计日志完整率。

## 7. 资料依据

- A2AJ 加拿大案例数据集：<https://huggingface.co/datasets/a2aj/canadian-case-law>
- CaLegal 加拿大优先技能包与运行时说明：<https://huggingface.co/datasets/dislove/calegal>
- Legal-Skills-Chinese：<https://github.com/THUYRan/Legal-Skills-Chinese>
- gonzih 法律技能包：<https://github.com/gonzih/skills-legal>
- CanLII Search+ 官方说明：<https://blog.canlii.org/2026/02/25/introducing-canlii-search/>
- CanLII Search+ 用户指南：<https://search-plus.canlii.org/user-guide.pdf>
- OpenJustice：<https://openjustice.ai/>
