# 专业 Skill、MCP 与 Agent 能力提升方案

## 1. 当前能力的真实边界

当前项目已经有本地加拿大法律 Skill 注册表，包含案例检索、法规检索、争点事实提取、案例法规关联、证据核验、风险评估和研究备忘录等能力；每次运行会保存输入、过滤条件、证据、结果和耗时。多 Agent 侧也有查询、检索、关联、引用核验、风险和规则仲裁的职责划分，以及庭审调度、对方代理人、审判长、证据和报告等角色白名单。这是可运行的受控原型，但距离工程级“专业能力平台”仍有明显差距：大部分 Skill 仍复用同一条混合检索链路，角色之间缺少独立的可测契约和版本；MCP 还没有实际客户端/服务端实现；部分庭审角色是受候选资料约束的模拟服务，而不是可独立交付、可替换、可评估的专业 Agent。

因此提升目标不是增加更多角色名称，也不是让模型任意调用更多工具，而是让每个专业职责有明确输入、允许的资料范围、输出 Schema、失败语义、质量指标、权限边界和人工复核点。MCP 只负责标准化地暴露受控资源和工具，不能替代权限模型、数据治理或法律专业复核。

## 2. 推荐的职责模型

应把 Agent、Skill、工具和 Workflow 分成四层。Workflow 负责庭审阶段、检查点、回滚和审批等确定性流程；Agent 只负责在不确定场景中根据当前状态选择下一项允许行动；Skill 是可独立测试的专业能力单元，例如“法规效力核验”或“证据链缺口识别”；工具是 Skill 访问检索、关系图、文档解析或计算能力的受限接口。这样“专业的人做专业的事”在工程上意味着：法域识别 Skill 不生成胜诉结论，法规效力 Skill 不评价证据真实性，证据 Skill 不篡改事实，风险 Skill 不创造法条或案例，报告 Skill 不绕过引用核验，规则引擎始终可以拒绝不合格输出。

建议优先形成以下可版本化的 Skill 契约。法域与程序 Intake Skill 只输出法域、法院层级、案由、事件日期、诉讼阶段和缺失字段；法规权威 Skill 只输出法规引文、条款定位、生效状态、修订日期、官方来源和核验状态；案例研究 Skill 只输出与争点匹配的案例候选、法院、日期、相似点、差异点和来源；引用验证 Skill 逐项确认引文、链接、原文片段、适用时点和是否需要人工复核；证据链 Skill 将“证据 -> 待证事实 -> 法律要件 -> 请求”建成可解释图并标识真实性、合法性、关联性、证明力和缺口；程序风险 Skill 专门检查管辖、主体资格、时效、送达、举证期限和救济路径；实体风险 Skill 只检查请求权基础和要件覆盖；执行风险 Skill 只检查履行能力、资产线索和保全条件；对方代理人与审判长 Skill 只能基于已确认事实和已核验资料生成质证或问题；报告 Skill 只汇总已通过核验的内容并输出免责声明。

每个 Skill 应有 Pydantic 输入输出模型、`skill_name`、语义版本、允许调用的工具集合、最大候选数、超时、失败状态和独立回归样本。输出中必须区分“已证实事实”“当事人主张”“推断”“待核验事项”，不能用同一个文本字段混写。这比让一个大模型以长提示词完成所有工作更容易测试、替换和审计。

## 3. MCP 的正确接入方式

当前项目不应为了“有 MCP”而把数据库、文件系统和网络访问全部暴露给模型。更合理的第一步是建立只读、最小权限的本地法律研究 MCP 服务，将现有内部能力适配为少量语义清晰的工具：受结构化过滤约束的法规/案例混合检索、正式案例法规关系查询、案例互引查询、引文与效力元数据核验、已授权卷宗文档解析、证据链图计算和指标计算。工具返回必须是结构化结果和可追溯来源，不返回数据库连接、任意 SQL 执行能力或未经授权的全文集合。

MCP 服务端应把身份、租户、案件 ACL、法域、只读范围和速率限制放在工具执行前，而不是依赖模型提示词。MCP 的架构将资源、工具和提示词作为显式能力暴露，服务端与客户端在初始化时协商能力；MCP 的授权规范也要求对受保护资源采用明确的授权模型，并强调不能透传令牌。可参考 [MCP 架构规范](https://modelcontextprotocol.io/specification/2025-11-25/architecture) 与 [MCP 授权规范](https://github.com/modelcontextprotocol/modelcontextprotocol/blob/main/docs/specification/2025-06-18/basic/authorization.mdx)。对本项目而言，客户端调用工具时应携带当前用户和案件上下文，服务端重新做权限判断；缓存键必须包含租户、案件、语料版本、嵌入模型版本、过滤条件和权限版本，禁止将私有卷宗命中结果复用于其他用户。

具有副作用的能力必须与只读研究工具隔离。后续如接入数据导入、批量索引、导出、发信、同步第三方法律库或写入案件卷宗的工具，应采用单独服务身份、显式审批、幂等键、操作预览、审计日志和人工确认；这些工具不能被庭审对话 Agent 自动调用。OWASP 对 LLM 应用的“过度代理”风险指出，过多功能、权限或自主性会使异常模型输出造成有害操作，因此本项目应坚持默认只读、按工具最小授权、预算限制和高影响操作人工审批的设计。[OWASP LLM06：过度代理](https://owasp.github.io/www-project-top-10-for-large-language-model-applications/2_0_vulns/LLM06_ExcessiveAgency.html)

## 4. 多 Agent 的工程化分工

不建议让多个 Agent 对同一法律问题投票后取多数，因为多数语言模型重复同一错误时不会变得更可靠。推荐使用“主管路由 + 专业 Worker + 规则仲裁”的模式：主管只根据结构化状态决定调用哪个专业 Skill；法规、案例、证据、程序和风险 Worker 分别输出受 Schema 约束的结果；引用与合规 Worker 有权阻断不带来源、超出法域或时效未核验的输出；最终规则仲裁器只聚合通过验证的字段。Supervisor/worker 的移交应保留发送方、接收方、输入摘要、工具结果和返回状态，避免由主管在自然语言中重述专业结果而丢失信息；这一点与 LangGraph Supervisor 的显式 handoff 工具设计相符，但项目没有必要为了采用这一模式立刻引入 LangGraph 依赖。[LangGraph Supervisor handoff 参考](https://reference.langchain.com/python/langgraph-supervisor/handoff)

庭审场景中，庭审状态机仍然拥有最高流程控制权。庭审调度器只允许在当前阶段调用对应的专业 Worker：庭前准备可调用 Intake、法规和案例研究；举证质证可调用证据链和引用核验；法庭辩论可调用实体/程序风险与类案差异分析；状态评估与检索可调用受控 ReAct；最终报告只能调用规则聚合与合规校验。对方代理人和审判长不是“真实裁判者”，它们只能提出基于候选资料的反驳、问题和待补材料，不能直接改变事实、证据状态或胜诉百分比。

## 5. 需要补齐的质量与治理能力

专业能力必须用可测指标约束。检索层应持续报告 Recall@K、MRR、nDCG、法规/案例分区覆盖、引用可溯源率、无效引用率和 P50/P95 延迟；法规权威 Skill 应报告官方来源覆盖率、效力状态准确率和时效核验覆盖率；证据链 Skill 应报告事实-证据映射覆盖率、人工纠正率和证据缺口召回率；风险 Skill 应报告四类风险的人工认可率、遗漏率和不当警报率；预测模块在拥有冻结真实标签后才报告 Brier score、ECE、可靠性曲线和时间外测试结果。没有足够金标或律师复核时，指标必须写为“无法估计”，不能用模型自评或单个演示案例替代。

模型输出安全不应只靠提示词。每次工具行动要做参数 Schema 校验、资料标识白名单校验、租户 ACL 校验、输入注入检测、最大候选限制、超时与最大步数限制；每次生成要做 JSON Schema 校验、引文存在性检查、来源可访问性检查和事实来源检查；每次最终输出要做免责声明、法域、法规效力、引用和人工复核检查。NIST 的 AI RMF 与其生成式 AI Profile 提供了将治理、测试、评估、验证和风险管理纳入系统生命周期的框架，可作为本项目记录风险、评测和上线门禁的参考，而不是把它当成一次性的合规清单。[NIST AI RMF](https://www.nist.gov/itl/ai-risk-management-framework)

## 6. 建议的实施顺序

第一阶段先重构现有 Skill 契约，不增加 MCP：为现有七个 Skill 补充输入输出 Schema、版本、错误码、独立测试集和质量指标，并把现有 `skill_runs` 审计记录扩展为可追溯的输入摘要、资料版本和引用核验结果。第二阶段实现法规效力、案例差异、证据链和程序时效四个高价值专用 Skill，先在后台影子运行，与现有综合检索输出比较，不影响现有用户流程。第三阶段把只读本地检索、正式关联、引用核验和证据链计算适配为 MCP 工具，并实现租户/案件级鉴权、能力发现、限流、审计和缓存命名空间。第四阶段在复杂研究与庭审“状态评估与检索”阶段接入受控 ReAct，限制它只能选择上述只读工具，并在无新增证据、超时、超步数或未通过引用核验时停止。第五阶段才考虑有副作用的工具，并要求每项操作经过预览、审批、幂等和可回滚验证。

外部开源法律 Skill 包可以作为需求清单、提示模板和测试案例的参考，不能不经审查直接获得生产权限。引入前必须逐项检查许可证、适用法域、维护状态、输入输出契约、是否依赖外部网络、是否会访问敏感数据、是否能提供引文来源、是否可在本地测试，以及是否符合当前项目的 ACL、审计和免责声明规则。最安全的方式是把通过审查的能力重写为项目自己的适配器和 Skill 契约，再纳入白名单和评测体系。

## 7. 验收标准

当每个专业 Skill 都有明确职责、可重复测试、版本化输入输出、证据绑定和失败语义；MCP 工具默认只读且在服务端强制租户/案件权限；复杂 Agent 的每一步可审计、可停止、可回退；庭审流程不能被任一 Worker 越权推进；并且人工复核和离线评测证明引用质量、检索质量和风险建议质量达到预设阈值时，项目才可以称为具备较强工程化专业能力。在此之前，最准确的定位仍是“以本地加拿大法律资料为基础、带受控检索与庭审模拟能力的法律研究原型”。
