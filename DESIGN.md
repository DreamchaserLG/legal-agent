# 多 Agent、Skill 协作与 MCP 工具设计

## 1. 实施目标

本次实现不把庭审状态机改造成自由自主 Agent。庭审仍由既有七阶段 Workflow 控制；新增的 Agent 能力只用于复杂研究和“状态评估与检索”中的资料缺口判断。目标是让查询理解、案例检索、法规检索、正式关联、引用核验和风险评估通过版本化 Skill 协作完成，并将可复用的只读能力设计为 MCP 工具契约。

基础实现采用项目现有 FastAPI、Pydantic、PostgreSQL、`skill_runtime_service`、`multi_agent_service` 和混合检索服务，不引入未经验证的第三方 Agent 框架，也不开放任意 SQL、文件系统、网络抓取或写入能力给模型。

## 2. 协作架构

```text
请求 + 用户/租户/案件 ACL
  -> 协作调度器
  -> 争点事实 Skill
  -> 案例检索 Skill + 法规检索 Skill
  -> 正式关联 Skill
  -> 引用核验 Skill
  -> 风险评估 Skill
  -> 规则仲裁
  -> 结构化研究结果 / 庭审阶段可用资料
```

调度器只根据事实缺口、已检索资料类型、引用核验状态和预算选择下一个 Skill，不生成法律结论。每个 Skill 使用既有运行时写入自己的审计记录；协作调度器再保存跨 Skill 的决策轨迹。简单检索继续走既有单 Agent + RAG，不额外触发协作链，避免增加延迟。

## 3. 协作判断规则

如果输入缺少法域、关键日期、法院或事实，先调用争点事实 Skill 并将缺口写入状态；案例和法规检索始终分开执行，避免其中一种资料压制另一种；当两类资料均存在时调用正式关联 Skill，区分“正式关系”与“语义候选”；当候选缺少引文、来源 URL 或效力状态时调用引用核验 Skill；仅在复杂研究、风险分析或庭审状态评估中调用风险 Skill；任一环节没有可核验证据、超过预算或 ACL 过滤后为空时，规则仲裁器停止链路并返回待补材料，而不是要求模型补造答案。

## 4. 状态与审计

协作状态以 Pydantic 模型表示，至少保存请求标识、租户、案件 ACL、意图、过滤条件、预算、事实缺口、已执行 Skill、每个 Skill 的运行标识、证据摘要、正式关联、引用核验、风险、停止原因和总耗时。模型不会保存私有思维链；每一步只记录可审计的决策理由，例如“缺少法规候选，调用法规检索 Skill”或“引用未核验，阻断报告生成”。

状态持久化使用独立协作运行表，不能覆盖现有 `skill_runs`、`multi_agent_runs` 或庭审状态表。状态中只保存脱敏摘要和资料标识，不复制无权限的全文。失败时保留已经验证的前序结果，并标识失败 Skill、错误类型和确定性回退路径。

## 5. MCP 工具设计

本次实现提供 MCP 就绪的内部工具适配层，而不是伪造完整 MCP 协议服务。适配层将工具的名称、参数 Schema、只读属性、所需权限和处理函数集中注册；未来接入标准 MCP SDK 时，只需把这些契约映射到 MCP 的工具定义和服务端鉴权，不改动领域服务。

第一批工具全部只读：混合检索工具接收查询、模块、资料类型、数量和白名单过滤，返回可追溯候选；正式关联工具接收已授权案例资料标识，返回案例到法规的关系、匹配原因和分数；引用核验工具接收候选资料，返回引文、来源 URL、日期和效力元数据是否完整；Skill 目录工具只返回已注册 Skill 的名称、版本和职责；协作研究工具只触发上述受控链路。工具参数采用 Pydantic Schema 校验，数量限制在安全上限，调用方必须提供用户、租户和案件 ACL 上下文。

工具注册表不提供通用数据库查询、任意 URL 获取、文件写入、索引创建、数据同步、模型训练或预测结果写回能力。任何未来有副作用的工具必须分离到新的审批型能力组，使用预览、幂等键、人工批准、完整审计和可回滚机制。

## 6. API 与兼容性

新增协作研究 API 和只读工具目录/调用 API，沿用既有 `/api`、会话认证、`tenant_id` 和 `case_acl` 约定。原有 `/api/skills/run`、`/api/multi-agent/run`、RAG API、庭审 API 和直接预测开关均保持兼容。协作 API 只返回结构化数据，不自动写入用户历史或改变庭审阶段；庭审服务后续可显式选择调用协作器。

## 7. 回滚与验收

协作功能通过配置开关启用。关闭开关时，协作 API 返回受控不可用状态，原有单 Agent、多 Agent 固定 DAG、Skill 和庭审功能均不受影响。MCP 工具适配层也只读，不会改变数据库内容。

验收包括：Skill 执行顺序和条件分支测试；ACL 过滤和空证据阻断测试；未知工具、非法参数、超时、最大 Skill 数和注入文本拒绝测试；工具返回只读性测试；协作结果中每项风险具有资料状态或“未检索到直接依据”标记；原有多 Agent 与庭审回归测试通过。性能验收单独记录协作链 P50/P95，并与单 Agent RAG 基线比较，不预设固定毫秒数。
