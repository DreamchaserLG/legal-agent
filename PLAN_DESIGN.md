# RAG 与 ReAct 设计及行动方案

## 1. 文档目的与边界

本文描述当前项目已经实现的 RAG 检索设计，以及后续将固定 DAG 扩展为受控 ReAct（Reason-Act-Observe）执行模式的行动方案。

本文中的“ReAct”指 Agent 的“受限决策 - 工具调用 - 观察”循环，不是 React 前端框架。当前前端仍是 FastAPI + Jinja + 原生 JavaScript；本方案不要求迁移前端框架。

原则：检索不生成法律结论；生成不编造引用；风险评估不改写事实；规则与权限校验优先于模型判断；所有输出均为研究辅助，必须由具备相应法域资格的律师复核。

## 2. 当前 RAG 总体设计

### 2.1 数据层

项目将加拿大法规、历史案例及其分块保存于 PostgreSQL。长文档采用 512 token 切块、64 token 重叠，分块保留原文、来源标识、法规或案例元数据、全文检索列和 1024 维向量。

在线向量检索使用 `pgvector` 的余弦距离；已启用真实 BGE-M3 向量时使用 HNSW 索引。全文检索使用 PostgreSQL `tsvector + GIN`。案例与法规之间的正式关联来自关系表中的标题、引文、别名和证据摘录，不把向量相似度伪装成引用关系。

主要数据关系：

```text
法规/案例原文
  -> 文档分块
  -> RAG 全文索引与向量索引
  -> 混合检索候选
  -> 案例-法规正式关联 / 案例互引关系
  -> 引用核验、风险绑定、庭审模拟或研究输出
```

### 2.2 查询理解与结构化过滤

入口 `hybrid_search` 先调用法律查询规划器，提取或扩展：原始问题、有效关键词、法律领域、争点、法规检索式、案例检索式和优先资料类型。

调用方可附带结构化过滤条件：法域、文档类型、法院层级、语言、日期范围。多 Agent 调用会先将过滤字段限制为白名单，防止任意字段透传到数据库查询。

法规与案例默认分区检索：

1. 生成法规查询和案例查询。
2. 按法规分区、案例分区分别执行全文检索和向量检索。
3. 在保留分区来源的前提下合并候选，避免法规结果被案例结果完全淹没，或反之。

### 2.3 召回与融合

每个资料分区并行进行两路召回：

| 通道 | 当前实现 | 作用 |
| --- | --- | --- |
| 全文通道 | `rag_search`，PostgreSQL `tsvector + GIN` | 处理法规编号、案例引文、法条名称、精确术语和关键词。 |
| 向量通道 | `vector_search`，BGE-M3/pgvector/HNSW 余弦检索 | 处理同义表述、案情描述和语义相近的段落。 |
| 融合通道 | RRF，默认 `k=60` | 消除两路原始分数不可直接比较的问题，以排名融合候选。 |
| 重排通道 | `rerank_legal_evidence` | 结合词项重合、领域重合、资料类型偏好、双通道命中和领域漂移惩罚重排。 |

当前默认融合权重为全文 `0.55`、向量 `0.45`。若向量提供方被识别为 `hash`，向量只作为诊断信息，实际融合权重自动降为 `0`，避免伪向量破坏法律检索排序。向量单通道候选还需要满足术语重合或最低相似度条件，降低无关语义漂移。

候选先按来源 URL、来源编码、主表记录或分块标识去重，再计算 RRF 分数：

```text
hybrid_score = 0.55 / (60 + 全文排名)
             + 0.45 / (60 + 向量排名)
             + 向量余弦强度修正
             + 资料类型与术语权威性修正
```

重排不是黑盒模型排序。它会保留 `term_overlap`、`domain_overlap`、`penalty`、`lexical_score`、`vector_score`、`hybrid_score` 和 `rerank_score`，便于定位结果为何排在前面。

### 2.4 上下文、关系与引用核验

RAG 返回的每项资料包含文档标识、标题、法规或案例引文、法院、裁判日期、效力状态、来源 URL、摘录和各阶段分数。

复杂查询会继续读取案例到法规的正式关联：关联记录包含法规标识、匹配别名、匹配来源、关联置信度和证据摘录。引用核验 Agent 检查引文或来源 URL 是否存在，并标记 `verified_metadata` 或 `needs_manual_citation_check`。检索不到直接依据时，输出必须写明“未检索到直接依据”。

### 2.5 RAG 在各业务环节的使用位置

| 业务环节 | RAG 使用方式 | 后续处理 |
| --- | --- | --- |
| 简单法条/案例查询 | 单 Agent 调用混合检索 | 返回排序后的可追溯资料，不直接生成结论。 |
| 复杂研究与风险分析 | 查询理解 -> 检索 -> 正式关联 -> 引用核验 | 风险输出绑定法规、类案和待补材料。 |
| 庭审模拟建档 | 对案例、法规分区并行检索 | 形成可选证据和法律依据目录。 |
| 庭审每轮提交 | 已选资料绑定到阶段状态 | 模拟对方抗辩和法官问话只能引用本轮候选标识。 |
| 最终报告 | 汇总已绑定的本地资料、阶段风险和评分 | 输出引用清单、风险矩阵和免责声明。 |

## 3. 当前已涉及的模块

| 文件或模块 | 当前职责 | 与 RAG/ReAct 的关系 |
| --- | --- | --- |
| `app/service/legal_query_planner_service.py` | 法律问题解析、检索词和分区查询规划 | RAG 的查询理解层。 |
| `app/service/rag_service.py` | 全文检索、RAG 分块维护 | RAG 的词法召回层。 |
| `app/service/vector_store_service.py` | 向量写入、pgvector/HNSW、余弦查询 | RAG 的语义召回层。 |
| `app/service/hybrid_retrieval_service.py` | 分区、并行召回、RRF 融合 | RAG 的主编排入口。 |
| `app/service/reranker_service.py` | 本地法律特征重排 | RAG 的精排层。 |
| `app/service/multi_agent_service.py` | Pydantic 共享状态、工具白名单、DAG、审计 | 当前受控 Agent 编排基础。 |
| `app/service/hearing_workflow_service.py` | 庭审阶段状态机、检查点、风险、最终聚合 | RAG 资料在模拟庭审中的使用层。 |
| `app/service/llm_service.py` | 结构化 JSON 响应、失败降级 | 仅用于受 Schema 和候选资料约束的模拟生成。 |
| `app/service/skill_runtime_service.py` | 技能注册与运行记录 | 可复用的工具/技能接入层。 |
| `schema.sql`、向量化脚本 | 分块表、全文、向量、RRF SQL 与离线维护 | 数据和索引层。 |
| `scripts/benchmark_retrieval.py`、`scripts/evaluate_case_retrieval.py` | 延迟和检索效果评估 | RAG 验收入口。 |

## 4. 当前 Agent 编排与 ReAct 的差异

当前复杂任务采用固定 DAG：

```text
query_agent
  -> retrieval_agent
  -> authority_agent
  -> verification_agent
  -> risk_agent
  -> arbitration_agent
```

简单问题使用 `single_rag_agent`。当前已有工具白名单、超时、最大步数、Token 预算、案例 ACL、审计记录和规则仲裁，因此具备实现 ReAct 的安全基础。

但当前不是完整的 ReAct：节点顺序基本固定，不能根据“观察结果”选择下一项最有价值的工具；也没有统一的行动计划、行动结果摘要和循环停止原因。因此不能将当前实现描述为开放式自主 Agent。

## 5. ReAct 目标设计

### 5.1 适用范围

不对简单法条检索和单点问答启用循环。它们继续使用单 Agent + RAG，以降低延迟和不可控性。

仅对复杂研究、风险分析、庭审模拟、跨法域请求和证据缺口补充启用“受控 ReAct”。ReAct 不能直接执行数据库写入、网络抓取、删除、训练或部署操作。

### 5.2 受限循环

```text
输入与权限检查
  -> 受控推理摘要（Reason）
  -> 白名单工具行动（Act）
  -> 确定性观察摘要（Observe）
  -> 规则仲裁与停止判断
  -> 下一行动或最终结构化输出
```

Reason 不保存或展示模型私有思维链。它只允许输出可审计的短结构化摘要：目标、已知事实、证据缺口、下一步目的、允许工具和停止条件。

Act 只能从现有工具语义中选择：查询理解、混合检索、正式关联查询、引用核验、证据核验、风险标注、规则仲裁。每次行动必须带已校验的参数，不能拼接任意 SQL、URL、文件路径或消息主题。

Observe 由程序生成，不由模型自由描述。它包含候选数、法规/案例比例、正式关联数、可追溯引用数、ACL 过滤数、未解决缺口、耗时和错误码。

### 5.3 共享状态扩展

在现有 `LegalGraphState` 上增量扩展以下字段，不改变已有字段含义：

```text
react_enabled: bool
react_iteration: int
react_max_iterations: int
action_plan: 已验证的行动列表
observations: 每次工具调用的确定性摘要
evidence_gaps: 事实、证据、法条、时效、权限缺口
stop_reason: 已满足、无新信息、超时、超步数、权限拒绝、需要人工复核
```

每项行动仍通过 `AGENT_TOOL_ALLOWLIST` 校验，审计继续写入现有 `multi_agent_runs`。庭审流程保留独立 `hearing_runs` 与检查点；需要复用 ReAct 时，仅将其作为“状态评估与检索”阶段的受控子流程。

### 5.4 行动选择规则

| 观察到的状态 | 下一步允许行动 | 禁止行为 |
| --- | --- | --- |
| 未识别法域、案由或日期 | 查询理解 | 直接输出法律结论或概率。 |
| 没有候选资料 | 一次扩展检索或请求用户补充 | 无限重试、调用外部未批准数据源。 |
| 有案例但无法条依据 | 法规分区检索、正式关联查询 | 将案例语义相似度标记为法条引用。 |
| 有法条但无类案 | 案例分区检索、法院层级过滤 | 编造类案。 |
| 引用缺少 URL/引文/效力状态 | 引用核验、标记人工复核 | 将未核验资料写为已验证。 |
| 证据链不完整 | 证据核验、风险标注 | 改写用户事实或虚构证据。 |
| 达到预算、无新资料或风险仍无法核验 | 规则仲裁并停止 | 继续循环或输出确定性结论。 |

### 5.5 停止与降级

每次循环先检查最大行动次数、总超时、Token 预算和候选上限。任一限制触发时，停止并输出已检索到的资料、未完成事项、停止原因和人工复核提示。

模型响应出现非法 JSON、未知工具、未知资料标识、超时或重试耗尽时，执行确定性降级：保留已验证候选，拒绝未授权行动，将问题写入审计，转由规则仲裁生成“无法完成自动判断”的结构化结果。

## 6. ReAct 实施行动计划

### 阶段 A：只读基线与验收口径

1. 固化当前 RAG 基线：按法规、案例、混合查询分别记录 Recall@K、MRR、nDCG、引用可溯源率、法规时效准确率、P50/P95 延迟和失败样本。
2. 固化复杂 Agent 基线：记录当前固定 DAG 的工具数、耗时、规则阻断率、引用未核验率和人工复核率。
3. 建立按法域、案由、法院层级、年份和资料完整度分层的评测集；没有独立金标时明确写“无法估计”。

产物：版本化评测配置、固定样本清单、基线 JSON 与说明文档。此阶段不改动在线逻辑。

### 阶段 B：状态和 Schema 增量扩展

1. 在 `multi_agent_service.py` 新增 Pydantic 行动、观察和停止原因模型。
2. 为现有状态增加可选 ReAct 字段，旧请求默认 `react_enabled=false`，保证旧 DAG 行为不变。
3. 复用现有 JSON Schema 服务校验受控推理摘要；模型只可选择预定义动作枚举。
4. 在审计中保存行动名称、已脱敏参数摘要、观察摘要、耗时、结果状态和停止原因；不保存私有思维链。

回滚：关闭 `react_enabled` 或对应配置开关，自动走现有 `GRAPH_ROUTING` 固定 DAG。

### 阶段 C：受控执行器

1. 新增内部 ReAct 执行器，只调用当前 Agent 工具白名单和服务函数。
2. 首先实现“缺法规依据”“缺类案”“引用缺失”“证据不足”四种行动规则，不开放自由工具调用。
3. 每次行动后由程序生成 Observation；只有 Observation 显示存在可补充空间时才允许下一轮。
4. 最终固定调用规则仲裁，形成证据、关联、引用核验、风险和缺口列表；不由 LLM 自由生成最终结论。

回滚：执行器异常时立即降级到固定 DAG，并标记 `react_fallback_to_dag`。

### 阶段 D：庭审模拟接入

1. 仅在“状态评估与检索”阶段调用受控 ReAct 子流程。
2. 输入限定为本庭审已确认事实、已选证据、已绑定法规、当前风险和 ACL；不得跨案件读取资料。
3. 输出只补充资料、引用核验结果和风险建议，不能推进庭审阶段或直接改变最终概率。
4. 对方代理人与审判长消息继续使用现有候选资料约束；ReAct 只负责确定下一项检索/核验动作。

回滚：庭审阶段继续使用当前 `_evidence_from_search` 和规则评估，不调用 ReAct 子流程。

### 阶段 E：质量门禁与灰度

1. 先以影子模式运行：同一复杂请求同时执行固定 DAG 与 ReAct，但只展示 DAG 结果，比较引用覆盖、候选增益、延迟和规则阻断。
2. 达到预先批准的指标后，对少量复杂研究请求开启 ReAct；简单检索始终保持单 Agent + RAG。
3. 若 P95 延迟、无效引用率、人工推翻率或权限拒绝异常超过阈值，自动关闭 ReAct 并回退固定 DAG。

## 7. 测试方案

| 测试类别 | 必测内容 |
| --- | --- |
| RAG 单元测试 | 查询规划、资料分区、去重、RRF、hash 向量降权、重排漂移惩罚、结构化过滤。 |
| RAG 集成测试 | PostgreSQL 全文检索、pgvector/HNSW、法规/案例分区、正式案例-法规关联、引用字段完整性。 |
| ReAct 单元测试 | 未知工具拒绝、非法 JSON 降级、超步数、超时、无新信息停止、ACL 过滤。 |
| ReAct 集成测试 | 四类证据缺口的行动选择、观察摘要、规则仲裁、DAG 回退。 |
| 庭审回归测试 | 阶段顺序、检查点、回滚、对方/法官消息、风险绑定、最终报告仅末阶段产生。 |
| 性能测试 | 单 RAG、固定 DAG、ReAct 三类请求的 P50/P95，分冷启动与热态记录。 |
| 人工复核 | 引用可溯源率、法规时效、风险建议可操作性、错误引用和事实篡改抽检。 |

## 8. 当前结论

当前 RAG 已具备可运行的混合检索、分区、关系关联、引用元数据和本地重排能力。当前多 Agent 已具备受控 DAG、安全白名单和审计，但尚未实现通用 ReAct 循环。

后续 ReAct 应作为复杂任务的可开关、可审计、可回退增强层，而不是替换简单 RAG 或允许模型自由调用工具。上线前不能以“有向量检索”或“有模拟概率”替代真实评测；必须以实际评测结果和律师复核记录决定是否扩大范围。

## 9. 项目问答记录

问：项目为什么使用 Agent 而不是 Workflow，支持使用的点在哪里？答：项目不应该把 Agent 和 Workflow 当成二选一。Workflow 负责把稳定、必须按顺序执行的过程固化下来，例如庭前准备到最终报告的七个阶段、每阶段的必填项、检查点、回滚和最终报告门禁；这部分如果交给自主 Agent 决定，很容易跳过举证质证或过早给出结论。Agent 只适合处理不确定性较高的局部任务，例如判断当前缺的是法规、类案、事实日期还是引用核验，决定在允许的检索和核验工具中下一步调用哪一个。当前代码实际已经采用这个取舍：庭审由 `hearing_workflow_service.py` 的状态机驱动，复杂研究由 `multi_agent_service.py` 的受控 DAG 补充查询理解、检索、正式关联、引用核验和风险绑定；未来 ReAct 也只应作为“状态评估与检索”阶段的受控子过程，而不是替换庭审 Workflow。

问：庭审模拟是一步步对话，是否 RAG 就够了？答：就核心交互而言，RAG 加状态机已经足够，而且是当前应优先使用的实现。用户每轮陈述后，系统用已确认的事实、已选证据和本地法规/案例候选生成对方抗辩与法官问话，再把风险和待验证项写回当前阶段；这不是“给 Agent 一个目标，让它自行完成案件”的任务。只有在资料缺口不明确时，例如既没有可用法规又没有类案、引文缺少来源、或需要判断先补哪类证据时，才需要受控 Agent 决定检索或核验顺序。将法官、对方代理人、书记员、证据核验、风险评估拆为不同角色是有意义的，但它们应受同一阶段状态和资料白名单约束，不能各自自由下结论；目前对方代理人与审判长的发言已受本次候选资料标识约束，其他专业角色仍处于规则化服务和规划接入阶段。

问：没有 MCP 工具调用，还能算 Agent 项目吗？答：可以，但需要准确表述。Agent 的最低工程定义不是“必须使用 MCP”，而是模型或规则能在状态、目标和观察结果的约束下选择下一步受限行动，并把行动结果反馈给后续决策。当前项目已经有查询理解、混合检索、关联查询、引用核验、风险标注和规则仲裁等内部工具语义，`AGENT_TOOL_ALLOWLIST` 会限制角色能调用什么，`LegalGraphState` 记录共享状态和审计，因此可以称为“受控 Agent/DAG 原型”。但它还不是完整的自主 ReAct Agent：节点顺序基本固定，工具是应用内部函数，不存在标准 MCP 客户端和服务端，也没有通用的观察后循环决策。面试时应说“项目实现了受控多 Agent 编排，MCP 作为后续安全工具协议接入”，而不是说“已经完成 MCP Agent 平台”。

问：项目整体流程是怎么规划的？答：整体从数据可信度开始，而不是从模型回答开始。法规 XML 和 A2AJ 案例先按原始记录保留，再按 512 token、64 token 重叠切块，写入全文索引和向量索引；在线查询先识别法域、资料类型、争点和日期过滤，再分别召回法规和案例的全文、向量候选，使用 RRF 融合并进行本地法律特征重排。复杂研究继续查询案例到法规的正式关联，核验每条资料是否有引文或来源 URL，再把可追溯资料绑定到实体、程序、证据和执行风险。庭审页面则把这些能力放入不可跳步的七阶段状态机，逐轮记录我方输入、对方抗辩、法官问话、证据和风险，最后才由规则聚合器输出辅助报告。未来工作重点不是增加更多自由生成，而是完成真实标签评测、正式引用覆盖、受控 ReAct 和生产级权限隔离。

问：庭审判决预测结果如何评估，如何确保有效？答：当前不能声称已经有经过验证的胜诉预测模型。现有最终百分比是由证据充分性、法律相关性、事实一致性、程序合规性、类案支持度和对方抗辩强度按可配置权重计算，再经 sigmoid 转换得到的“模拟状态指标”；页面和报告已经标记为未校准，它的作用是提示材料缺口和复核优先级，而不是预测真实裁判。要证明预测有效，必须先冻结“胜诉、部分胜诉、败诉、和解、程序终结”等标签口径，按时间切分训练集、验证集和测试集，避免同一案件或后续信息泄漏；随后分法域、案由、法院层级和年份报告 Brier score、ECE、可靠性曲线、PR-AUC、Recall@K、MRR 和人工律师复核推翻率。只有在独立时间外测试集达到事先批准的性能和校准阈值、并且样本量足够时，才可将校准后的概率提供给专业人员，仍需保留置信区间、适用范围和律师复核提示。

问：案例与法律法规关联采用哪些数据，如何保证正确性；非官方案例数据是否会削弱效果？答：法规侧优先使用加拿大 `laws-lois-xml` 等官方或政府发布的法规 XML，案例侧当前主要来自 A2AJ 的加拿大案例数据集，并保留来源标识、原始引文、URL、法院和日期等元数据；两者不是只靠向量语义相似度关联。项目会从案例正文和结构化字段提取法规引文、标题和别名，再把匹配规则、匹配分数和证据摘录写入案例到法规关系表，案例互引也保留原始引用与解析结果。非官方或聚合案例源确实会降低来源权威性、完整性和时效保证，因此它只能作为研究语料，不能自动等同于官方裁判库；系统应在每项结果上显示来源和核验状态，对缺少原始来源、引文或效力信息的资料标记为人工复核，最终结论不能仅依赖未核验数据。后续应对高价值结果增加官方页面回链、版本校验、重复源比对和抽样人工复核，而不是用更高的向量分数掩盖数据来源问题。

问：项目中遇到过哪些难题，如何解决？答：最典型的问题是早期案例检索对长标题和泛化关键词容易退化，全文查询会扩展出过多低选择性词并扫描大量分块，曾出现单次查询约二十秒的情况。解决方式不是盲目提高硬件或把所有内容交给模型，而是先检查查询计划和数据库执行路径，再改为精确全文命中优先、零命中才前缀回退，并排除低选择性词；同时把案例与法规分区、RRF 融合和本地重排拆开观察，避免一个通道的异常拖垮全链路。另一个难题是历史数据中存在 hash 向量和真实语义向量并存，如果把它们同等参与融合会制造看似相关的噪声，因此现在会识别 hash 提供方并把其向量权重降为零，只保留为诊断信息。还有案例到法规关系并没有来自 A2AJ 的现成金标，项目因此保留匹配证据、来源和分数，并在文档中明确覆盖率不等于准确率，后续需要独立抽样和律师复核，而不是直接宣称关联正确率。

问：为什么单 Agent 不够，多 Agent 工作流是什么样，能否支持多人同时使用和共享向量缓存？答：简单法条检索、案例检索和单点问答不需要多 Agent，当前就由 `single_rag_agent` 一次完成查询理解、混合检索和引用元数据检查，这样延迟更低、链路更容易验证。复杂研究或风险分析才使用多 Agent，因为检索、正式关联、引用核验、风险标注和规则仲裁的职责不同：当前工作流是 `query_agent -> retrieval_agent -> authority_agent -> verification_agent -> risk_agent -> arbitration_agent`，最后的规则仲裁可以阻断无证据、跨法域未接入或引用未核验时的结论。多人使用时，PostgreSQL 中的公共法规和案例向量索引可以共享，查询向量和检索候选可以在未来按模型版本、语料版本、过滤条件和租户范围做短期缓存；但私有卷宗绝不能共享缓存键或返回结果。当前实现已有 `tenant_id`、`case_acl`、审计表和输出过滤，属于生产隔离的起点，而不是完整生产级隔离；要达到生产级，还需要数据库行级安全、租户/案件级数据分区、每次工具调用重新鉴权、加密、缓存命名空间、审计不可篡改、限流和删除策略。

问：项目如何处理大模型幻觉，如何证明真的降低了幻觉？答：项目并不能证明“消除了幻觉”，只能通过工程约束降低未经证据支持的输出概率。具体做法是让检索结果携带文档标识、引文、来源 URL、法院、日期和效力状态；模拟发言只能引用当前候选资料标识；风险必须绑定法规和类案，找不到时明确写“未检索到直接依据”；引用核验会标记缺少引文或来源的资料；规则仲裁会在无证据、超时、跨法域知识库缺失时阻断实体结论；模型输出采用 JSON Schema 校验和失败降级。是否真的有效不能凭主观感受判断，应在人工标注的测试集上比较“有依据主张比例、引用可溯源率、虚构案例/法规率、无效或过期法规率、事实篡改率、引用与原文蕴含一致率”，并统计律师抽查推翻率；同时保留失败样本和版本号，比较加入约束前后的结果，才可以说某项约束降低了某类错误。
