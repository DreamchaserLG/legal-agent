# 加拿大法律 Agent：算法、工程与面试复盘

## 1. 项目定位

这是一个以加拿大法律法规与案例为语料的本地法律研究 Agent 原型。目标不是替代律师或输出确定性裁判结果，而是把“自然语言问题 -> 法律检索 -> 案例/法规关联 -> 证据核验 -> 风险复核草稿”做成可追溯的闭环。

系统使用 PostgreSQL 作为事实数据源，pgvector 存储语义向量，BGE-M3 生成 1024 维归一化向量，HNSW 完成近似最近邻召回；全文 GIN 索引负责关键词召回，RRF 融合两路候选。生成层使用 Qwen OpenAI-compatible 服务，但法律结论必须被本地证据约束。

当前阶段是工程原型，不应宣称为法律意见系统或“类人裁判预测器”。风险输出被定位为律师复核草稿。

## 2. 问题拆解

法律检索的难点不只是语义相似：

1. 同一概念可能跨越案例、法规、法院层级、法域和历史版本。
2. 长裁判书不能整篇嵌入，否则模型输入被截断、召回粒度过粗、证据无法定位。
3. 仅靠向量相似度容易漏掉精确法条、案号、法院缩写和日期；仅靠关键词又无法处理同义表达。
4. 法律问答的错误成本高，模型生成内容必须能回到原始案例或法条片段。
5. 加拿大普通法、联邦/省级法规与魁北克民法不能直接复用中国法规则或面向其他法域的技能提示词。

因此系统把问题拆为数据层、检索层、关联层、Agent 技能层、质量评测层五部分。

## 3. 数据层与全量导入

### 3.1 数据模型

原始数据首先进入 `source_items`：

- `source_code + source_uid` 是幂等自然键，防止重跑造成重复。
- `raw_text` 保存正文，`raw_json` 保存上游元数据，例如法院、引注、语言、被引案例。
- `legal_cases` 是从原始案例同步出的结构化案例表。
- `legal_rules` 保存法规及条款级信息。
- `case_rule_relations` 保存案例到法规的正式关联，包含 `match_score` 和 `match_reason`。

这样把“可复原的原文”和“服务查询的结构化实体”分离：重跑导入不会破坏应用层 ID，关联与向量可以按内容哈希重建。

### 3.2 A2AJ 导入算法

A2AJ 主数据集的实际规模是 225,807 条案例。早期逐页调用 datasets-server rows 接口，在 offset 14,618 后发生 TLS 失败；解决方案改为按 29 个法院/机构配置下载 Parquet 分区。

每个分区在 `a2aj_parquet_checkpoints` 保存：

```text
(config_name, last_processed_row, completed, updated_at)
```

导入流程：

1. 读取该分区检查点。
2. 顺序迭代 Parquet record batch。
3. 跳过已处理行，从 `last_processed_row` 继续。
4. 清洗文本并按 `source_uid` upsert。
5. 每个批次提交检查点。

时间复杂度为 O(N)，空间复杂度为 O(B)，其中 B 是 PyArrow 批大小，而不是整个 Parquet 文件大小。即使进程中断，最多重做最后一个批次；由于 `source_uid` 唯一约束，重做是幂等的。

### 3.3 数据清洗问题与修复

实际导入暴露了三个典型 ETL 问题：

| 问题 | 表现 | 根因 | 修复 |
| --- | --- | --- | --- |
| rows TLS 失败 | 固定 offset 无法继续 | 远程分页连接不稳定 | 使用镜像优先、官方回退的 Parquet 分区与数据库检查点 |
| 空正文记录 | 无法产生有效分块 | 上游仅提供元数据 | 写入前跳过空正文，历史空记录清理 |
| NUL 字符 | RAD 分区在同一断点无限重试 | PostgreSQL `text/jsonb` 不允许 `\\x00` | 在全局 `repair_text` 中删除 NUL，使正文和 JSON 走同一清洗入口 |
| 5 万字符截断 | 长裁判书后半段丢失 | 旧默认上限不适合全量语料 | 全量入口 `max_text_chars=0`，保留全文并幂等覆写旧记录 |

其中 NUL 修复的关键是放在公共文本函数，而不是只修 A2AJ 导入器。因为 NUL 也可能出现在标题、摘要、关键词、审计日志和 JSON 元数据中；统一入口能避免同类问题在其他路径复发。

## 4. 分块、嵌入与向量索引

### 4.1 Token 分块

系统使用 `RecursiveCharacterTextSplitter`，优先按段落、换行、中文/英文句末符号切分；长度函数由 BGE-M3 tokenizer 计算，而不是字符数。

- chunk size：512 tokens
- overlap：64 tokens
- 超长片段：按 token 窗口兜底切分

如果文档 token 序列为 `x_1...x_n`，窗口长度为 C、重叠为 O，则第 k 块起点约为 `k * (C - O)`。重叠能防止争点、例外条件或法条要件恰好落在边界时丢失上下文。

存储结构为：

```text
案例/法规实体 -> rag_chunks(chunk_index, text_content, content_hash) -> rag_chunk_embeddings
```

`content_hash` 使向量迁移增量化：只有模型版本、向量提供方或正文哈希改变的分块才重新编码。

### 4.2 BGE-M3 与余弦相似度

BGE-M3 输出 1024 维向量。设查询向量为 q、文档向量为 d，余弦相似度为：

```text
cos(q, d) = (q · d) / (||q|| * ||d||)
```

写入前进行 L2 归一化后，`||q|| = ||d|| = 1`，因此余弦相似度等价于点积。pgvector 使用 `vector_cosine_ops`，查询时以 `<=>` 计算余弦距离。系统校验向量维度为 1024、内容哈希一致且范数接近 1。

### 4.3 HNSW

HNSW（Hierarchical Navigable Small World）将向量构造成多层近邻图：

1. 高层图稀疏，用于快速从远处接近目标区域。
2. 逐层下降到更稠密的图。
3. 底层维护候选邻居，得到近似 Top-K。

当前参数：`m=16`、`ef_construction=64`、查询 `ef_search=80`。`m` 越大，图连通性和召回通常更好，但索引占用和构建成本更高；`ef_search` 越大，召回更高但查询更慢。HNSW 的实际查询近似为亚线性，但严格复杂度依赖数据分布和参数，不能简单宣称 O(log N)。

工程上还遇到过 PostgreSQL 统计信息过期，优化器选择 btree 扫描再排序，导致检索变慢。每批向量写入后执行 `ANALYZE rag_chunk_embeddings`，使优化器重新选择 HNSW 路径。纯 SQL HNSW 的历史热态均值约 1.17ms；端到端还包含模型编码、过滤、融合和重排，不应混为一谈。

## 5. 混合检索与排序

### 5.1 两路召回

系统将案例与法规分路检索，防止法规片段挤占案例候选：

- 词法通道：PostgreSQL `tsvector + GIN`，擅长案号、法条名称、精确短语。
- 语义通道：BGE-M3 + pgvector HNSW，擅长同义表达和事实描述。

结构化过滤在召回前执行，例如法域、文书类型、法院级别、语言和日期范围。这比先召回再过滤更稳定，也减少无关候选。

### 5.2 RRF 融合

对于候选文档 d，词法排名为 `rank_lex(d)`，向量排名为 `rank_vec(d)`，RRF 分数为：

```text
RRF(d) = w_lex / (k + rank_lex(d)) + w_vec / (k + rank_vec(d))
```

当前默认 `w_lex=0.55`、`w_vec=0.45`、`k=60`。RRF 只依赖名次，不要求两个分数处于同一量纲，因此对 `ts_rank` 与余弦分数更稳健。融合后再依据法律查询计划、标题重合和来源类型做轻量重排。

### 5.3 关联与匹配度的正确解释

“匹配度”至少有三种不同指标：

1. 原始源同步率：`legal_cases / A2AJ source_items`，衡量 ETL 是否完成。
2. 关联覆盖率：至少有一个 `case_rule_relations` 的案例占比，衡量案例-法规图是否稠密。
3. 检索准确率：在人工标注问题集上的 Recall@K、nDCG 或 MRR，衡量搜索结果是否正确。

它们不能互相替代。当前全量源还未同步时，正式案例关联覆盖率只代表旧的 1,018 条案例，不能声称全库达到 90%。历史 50 条标题查询中的 92% 命中率仅是小样本基准，不是全量数据质量结论。

## 6. 加拿大法律 Agent 技能层

为避免直接执行未审计的第三方技能包，项目实现了本地加拿大技能运行时：

```text
用户输入
  -> 注入指令检测
  -> 技能路由
  -> 结构化事实提取 / 混合检索
  -> 正式案例-法规关系读取
  -> 证据清单
  -> 风险或备忘录草稿
  -> skill_runs 审计记录
```

已实现七个技能：

- `case_retrieval_ca`
- `statute_retrieval_ca`
- `issue_fact_extraction`
- `authority_linking`
- `evidence_verification`
- `risk_assessment_ca`
- `research_memo_ca`

每次运行都记录技能版本、过滤条件、候选证据、结果 JSON 和耗时。输入命中“忽略此前指令”“泄露系统提示”等注入模式时，直接阻断，不执行检索或外部操作。

风险技能不输出“会胜诉”的确定性结论，而是输出事实缺口、支持与相反因素、置信区间和律师复核标记。这是高风险场景中将模型能力限制为证据整理与风险提示的设计。

## 7. 性能分析与优化思路

端到端延迟可以拆成：

```text
T_total = T_query_parse + T_embedding + T_hnsw + T_lexical + T_rrf/rerank + T_serialization
```

优化不能只看 HNSW：

- HNSW SQL 查询可以是毫秒级。
- BGE-M3 首次加载是冷启动，可能达到秒级；服务常驻后可显著下降。
- 全量向量迁移与在线推理竞争 GPU 时，embedding 阶段会变慢。
- 长关键词的低效 `LIKE` 全表扫描会造成秒级甚至十秒级延迟，已改为 tsvector + GIN。

常见优化策略：模型预热、批量编码、离线迁移和在线查询隔离 GPU、使用连接池、只取 Top-K 证据、对相同查询做短 TTL 缓存、使用 `EXPLAIN ANALYZE` 验证 HNSW 执行计划。性能报告必须分别给出冷启动、纯数据库、模型常驻和完整 API 四种口径。

## 8. 质量门禁与评测

数据质量门禁：

- `source_uid` 无重复。
- 正文非空。
- 结构化案例必须能回溯 `source_item_id`。
- 分块不得超过 512 tokens。
- 向量必须是 1024 维、内容哈希一致、范数有效。
- HNSW 索引存在，查询计划没有退化为全表排序。

检索评测应构建冻结的人工标注集，覆盖联邦、省级、劳动、合同、刑事、人权和行政法。每题标注应命中的案例、法规、段落和不可回答条件，输出 Recall@K、nDCG、证据定位率、无证据主张率和 P95 延迟。

预测模型只有在拥有合法、可追溯、按时间切分的人工标签后才可以训练。评估应包含 Brier score、校准曲线、分法域偏差和拒答正确率。没有这些条件时，系统只做证据检索和风险草稿，不做概率预测承诺。

## 9. 面试可讲的算法与工程亮点

### STAR 叙述

- Situation：法律语料长、来源不稳定、案例和法规关联稀疏，纯关键词检索慢且遗漏同义表达。
- Task：构建一个可恢复、可审计、低延迟的加拿大法律检索与分析闭环。
- Action：用 Parquet + checkpoint 设计幂等 ETL；用 tokenizer 分块、BGE-M3、pgvector HNSW、GIN 和 RRF 设计混合检索；用证据链与审计表限制生成；针对 NUL、截断、统计信息过期逐项修复。
- Result：建立了断点续传与数据质量门禁，纯 HNSW SQL 热态进入毫秒级；同时明确区分 ETL 覆盖率、关联覆盖率和检索准确率，避免错误指标叙述。

### 高频追问

1. 为什么不用纯向量检索？
   因为法律任务有案号、条款号、法院简称等精确项；纯向量容易丢失这些关键约束。使用词法和向量的互补召回，再用 RRF 融合。

2. 为什么 chunk size 是 512 tokens？
   在证据粒度、模型上下文和向量语义稳定性之间折中。必须使用 tokenizer 精确计数，字符数无法保证模型输入上限。

3. 为什么选择 HNSW？
   适合高召回、低延迟的在线近邻查询。代价是构建慢和内存占用更高，因此离线重建、参数调优、增量写入和查询计划验证同样重要。

4. 如何保证导入幂等？
   用来源稳定 UID 做唯一键，checkpoint 只表示进度而不表示唯一性。重复批次会 upsert 覆盖而非新增。

5. 如何避免法律幻觉？
   生成前要求检索证据，输出保留原文片段和链接，证据验证技能禁止无来源主张；证据不足时拒答或降级为待核验清单。

6. 为什么不直接接商业法律平台或 CanLII？
   必须遵守许可证、账户、API 和数据处理边界。没有书面授权不能抓取、绕过登录或把案件材料发送到外部服务。

## 10. 当前限制与下一步

截至本文档更新时，A2AJ 全量导入仍在运行，正式案例同步、全库法规关联、全量 RAG 重建和 BGE-M3 迁移需要等 225,807 条源案例完成后执行。当前正式案例关联覆盖率只反映旧数据，不能作为全量结果。

下一阶段应先完成 P0 数据闭环和冻结评测集，再以评测指标调整 HNSW 参数、RRF 权重和关系抽取阈值；最后才评估风险标签训练和最小权限 MCP 适配。
