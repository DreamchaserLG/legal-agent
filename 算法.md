# 加拿大法律 RAG 系统：数据、算法与模块说明

> 本文以当前仓库的实际实现为准，时间点为 2026-09-13。它区分“已实现”“正在执行”和“建议后续实现”，不将设计设想描述为线上能力。系统用于法律研究辅助和证据检索，不替代加拿大执业律师的法律意见或裁判结论。

## 1. 系统要解决什么问题

输入是一段自然语言案情、法律问题或已知引文；输出应包含可核验的法规、案例片段、相似度/混合排序分数、正式案例-法规关系及其证据。核心目标不是让模型凭记忆回答，而是让生成或分析过程受本地证据约束。

系统有两条互补链路：

1. **关系链路**：从案例正文中的法规引文、案例引用字段中抽取显式关系，存储来源、原文证据和匹配分数。它回答“这个案例明确引用了哪部法”。
2. **检索链路**：从切片语料中按关键词和语义相似度检索。它回答“与该问题最相关的案例或法条可能是什么”。

二者不能互相替代。向量相似度只代表语义接近，不能证明法律上的引用、效力或适用关系；显式引文关系虽可追溯，但会漏掉未按标准格式写出的隐含关联。因此，查询结果应同时展示关系来源、证据片段和检索分数。

## 2. 当前模块关系

```text
A2AJ 案例 Parquet / A2AJ 法规 / laws-lois XML
              |
              v
  导入脚本与清洗层（source_items、legal_cases、legal_rules、canada_laws）
              |
              +--> 引文解析与标准化 --> 显式关联表（案例<->案例、案例<->法规、法规<->法规）
              |
              v
 rag_service：构建文档、token 精确分块、保存 rag_chunks + tsvector
              |
              v
 embedding_service：BGE-M3 批量编码、归一化向量
              |
              v
 vector_store_service：rag_chunk_embeddings（pgvector VECTOR(1024)）
              |
              +--> 全文检索 rag_search（GIN/tsvector）
              +--> 语义检索 vector_search（cosine，最终使用 HNSW）
              v
 hybrid_retrieval_service：候选合并、RRF、规则重排
              |
              +--> rag_service.build_rag_context：上下文、引用编号
              +--> skill_runtime_service：本地法律技能、审计记录 skill_runs
              +--> analysis_service / API：界面或模型回答所需的证据包
```

主要代码位置：

| 模块 | 职责 |
| --- | --- |
| `scripts/ingest_open_legal_data.py`、`scripts/run_a2aj_full_pipeline.py` | 导入 A2AJ 与本地开放法律数据，支持分批和检查点。 |
| `scripts/build_a2aj_links.py` | 只读本地数据，生成案例->案例、案例->法规、法规->法规 CSV 关系文件。 |
| `app/service/canada_case_law_service.py` | 保存案例-法规关联的别名、来源、分数与原文证据片段。 |
| `app/service/rag_service.py` | 文档清洗、精确 token 分块、`rag_chunks`、全文检索及 RAG 上下文。 |
| `app/service/embedding_service.py` | 本地 BGE-M3 缓存加载、GPU/CPU、批量编码、归一化和 OOM 降级。 |
| `app/service/vector_store_service.py` | 幂等写入向量、pgvector 余弦搜索、结构化过滤、HNSW 配置。 |
| `app/service/hybrid_retrieval_service.py` | 关键词与向量候选、RRF 融合、法律证据重排。 |
| `app/service/skill_runtime_service.py` | 本地可审计技能编排、输入过滤、证据持久化。 |
| `scripts/benchmark_retrieval.py`、`scripts/evaluate_case_retrieval.py` | 延迟、执行计划、Recall@K、MRR 等检验。 |

## 3. 文档获取、清洗与可追溯性

### 3.1 数据获取

主要输入为已下载的 `a2aj/canadian-case-law` 案例分片、`a2aj/canadian-laws` 法规数据和 `laws-lois-xml` 法规 XML。导入操作保留来源代码、来源唯一标识、标题、URL、发布日期、原文和原始元数据；不会以向量替代原文。

案例的正式标识优先使用 `citation_en`，缺失时使用 `citation2_en`。案例之间的引用来自 `cases_cited`；法规引文从 `unofficial_text_en` 等正文中提取。引文识别至少覆盖 `R.S.C./RSC`、`R.S.O./RSO`、`S.C./SC` 的年份和章节格式，并对大小写、点号和空白做标准化。每条关系都保留引用方 ID、原始引文、标准化引文、匹配到的目标 ID；未匹配的记录也保留为空目标，供覆盖率排查。

### 3.2 清洗原则

1. 保留原始全文和原始 JSON，清洗后的正文只作为检索输入，不能覆盖原件。
2. 删除 HTML/无效空白、修复常见编码问题，拼接标题、摘要和正文形成可检索文档。
3. 用 `(source_table, source_id, chunk_index)` 唯一约束防止重复切片；用 `content_hash` 判断正文是否变化。
4. 元数据单独保存：`jurisdiction`、`document_type`、`court_level`、`language`、`citation`、日期和来源 URL，不把结构化信息只埋在文本里。
5. 增量更新时只重建正文哈希变化或缺失向量的切片，避免全表重算。

这种设计让任何命中结果都可回溯为“哪个数据源的哪一篇文档的第几个分块”，并能继续跳转原始 URL 或全文。

## 4. 为什么采用 512 token 分块和 64 token 重叠

### 4.1 当前规则

实现位于 `rag_service.split_into_chunks`：

1. 使用 BGE-M3 的 Hugging Face tokenizer 计数，而不是按字符数或词数估算。
2. 若全文不超过 512 token，全文作为一个块。
3. 超过时使用 `RecursiveCharacterTextSplitter`，优先分隔符依次为：双换行、单换行、中文句末符号、英文句末符号、空格、最后才是无分隔符。
4. `chunk_size=512`、`chunk_overlap=64`。递归切分后若某块仍超长，按 tokenizer ID 的窗口精确兜底切分；窗口步长是 `512 - 64 = 448 token`。
5. 每个块带有原文档 ID 和从 0 开始的 `chunk_index`，而不是对整篇案例只生成一个向量。

### 4.2 512 的取舍

512 token 通常能容纳一个或数个完整法律段落，并保留争点、规则、事实或结论的局部上下文。它又足够短，使 BGE-M3 能对查询和段落形成较聚焦的表示，GPU 批处理效率和 PostgreSQL 存储开销也较可控。

不采用“整篇文书一个向量”：长判决可能数万 token，一个向量会混合程序史、事实、争点和结论，查询命中后也无法定位证据。

不默认采用更小的 128/256 token：边界更多、向量数量和索引体积急剧上升，法规条号与其适用条件或例外更容易被拆开，召回结果也更碎片化。短问答、定义检索或细粒度段落引用可以另建 256 token 实验索引，但应以 Recall@K、MRR 和人工盲评胜出为前提。

不默认采用 1,024 token 以上：每块会混入过多主题，命中片段难读，编码与传入 LLM 的上下文成本更高。对于结构非常清晰、条款很长的法规，可在未来增加“条/款优先切分”的自适应策略，而不是全局把窗口扩大。

### 4.3 为什么重叠 64 token

64 token 是块长度的 12.5%。法律规则往往在段首定义、在段末给出条件或例外；没有重叠时，引用、否定词、主体或条件可能被分到相邻块，降低召回。64 可使相邻块共享约一个短段落的语义锚点，同时重复数据约为 `64 / 448 = 14.3%`，远低于 128 或 256 token 重叠的存储和编码重复。

不采用 0 重叠：边界损失明显，尤其是跨段落的法规引用和判决理由。

不采用 128/256 重叠：可能提高极少数边界问题的召回，但会显著增加向量数量、GPU 时间、表大小、HNSW 内存和重复证据。是否调整必须用固定评测集对比 Recall@12、MRR、nDCG、P95 延迟和人工证据完整度，不能只凭直觉。

## 5. 向量化与存储

### 5.1 向量生成

当前生产迁移使用 `BAAI/bge-m3` 与 `sentence-transformers`，输出 1024 维向量。模型加载优先使用本地缓存，Hugging Face 镜像变量为 `HF_ENDPOINT=https://hf-mirror.com`；运行时设置 `max_seq_length=8192`。`model.encode(..., normalize_embeddings=True)` 进行 L2 归一化，因此余弦相似度可稳定比较不同文本的方向，而不受向量长度影响。

嵌入表 `rag_chunk_embeddings` 以 `chunk_id` 为主键，记录 provider、model、dimension、content hash、JSON 兼容字段和 PostgreSQL `VECTOR(1024)`。查询始终按 provider/model 筛选，避免旧 hash 向量、其他模型向量与 BGE-M3 混检。

GPU 工作器当前按 512 个切片调度，嵌入子批大小为 32；批次结束写 JSON Lines 和中文进度日志。显存不足时，嵌入服务捕获 CUDA OOM、清空缓存并重载 CPU 模型继续执行。幂等的缺失向量查询和内容哈希使停止、切换 CPU/GPU、重启后不会重复写入已完成项。

### 5.2 余弦相似度

pgvector 使用 `<=>` 余弦距离，结果分数为：

```text
vector_score = 1 - cosine_distance(query_vector, chunk_vector)
```

归一化后余弦相似度主要衡量语义方向夹角。它不等于法律正确率、引用关系概率或胜诉概率，所以界面和下游模型只能把它作为排序信号，不能将其解释为事实置信度。

### 5.3 当前进度与索引时机

截至本文编写时，法规切片 BGE-M3 向量已完成；案例切片仍在 GPU 全量迁移中。写入期显式延后 HNSW 构建：增量插入会持续维护图结构，速度慢、磁盘膨胀且可能留下未完成索引。待全量向量完成、维度和行数验收通过后，再用独立连接 `CREATE INDEX CONCURRENTLY` 构建 HNSW，不阻塞业务读写。

## 6. 检索如何工作

### 6.1 先做结构化过滤

查询可带 `jurisdiction`、`document_type`、`court_level`、`language`、`date_from`、`date_to`。这些字段由 B-tree/组合索引和 SQL `WHERE` 子句先过滤。例如用户问 Ontario 上诉法院 2020 年后的雇佣法案例，应先缩小到 Ontario、案例、目标法院和日期区间，再进行词法/向量排序。

这样做的原因是：向量相似度不能可靠地区分法院层级、法域或生效时间；先过滤可减少候选量，降低延迟和跨法域误召回。

### 6.2 全文通道

`rag_chunks.search_vector` 保存 PostgreSQL `tsvector`，GIN 索引支持关键词、引文、法条号和罕见专有名词检索。`rag_search` 采用“精确匹配优先，未命中再宽松回退”的 SQL：有精确候选时不执行泛前缀 OR 排序，避免常见词导致全索引扫描。

全文检索适合精确 citation、法条章节、当事人名和法院缩写；其弱点是同义表达、改写和中文问题转英文法律术语。

### 6.3 向量通道与 HNSW

向量查询先计算 query 的 BGE-M3 向量，再按余弦距离从近到远返回候选。全量迁移完成后使用 HNSW：

- `m=16`：每个节点最多约 16 条图连接，是召回、内存和构建时间的保守平衡。
- `ef_construction=64`：建图时每个节点的候选搜索宽度，数值越大通常越准也越慢。
- `ef_search=80`：在线查询候选宽度，可在不重建索引的情况下调高以改善召回，代价是延迟。

HNSW 是近似最近邻，不保证精确 top-K。必须将未索引小样本与精确顺序扫描对照，记录 Recall@K，再选择 `ef_search`；不能仅根据单次毫秒延迟宣称质量。

### 6.4 混合检索、RRF 与重排

当前 `hybrid_search` 分别获取词法和向量候选，默认各通道候选上限为 24，最终上下文通常取前 8 条。使用 RRF（Reciprocal Rank Fusion，倒数排名融合）：

```text
score(d) = 0.55 / (60 + rank_lexical(d))
         + 0.45 / (60 + rank_vector(d))
         + authority_boost(d)
         + small_vector_margin(d)
```

其中 `60` 是 RRF 平滑常数；RRF 融合名次而不是直接融合不同量纲的原始分数，因此对 `ts_rank` 和余弦分数更稳健。候选还需通过有意义查询词重叠、低相似度纯向量候选拦截和权威性规则，再调用本地 `rerank_legal_evidence` 排序。若检测到旧 hash embedding，向量通道仅作为诊断，不将纯向量结果当作正式证据。

查询会按案例和法规等分区分别检索，避免“法规数量或案例数量较大的一侧”挤占另一侧结果。结果中保留 `lexical_score`、`vector_score`、`hybrid_score`、来源、切片编号和元数据，便于解释排序。

## 7. RAG 问答流程

RAG 的目标是“检索增强生成”，不是把整个数据库送给模型。当前流程如下：

1. 接收案情或问题，清洗文本；分析层提取关键词、可能的法院、日期、引文和争点。可配置使用 LLM 辅助关键词，但本地规则可作为回退。
2. 将可确认的条件转为结构化过滤；对案例和法规分区执行全文与向量检索。
3. 融合、去重、重排，选取数量受限的高质量切片，并建立 `R1...Rn` 引用列表。每条引用包含标题、来源、URL、片段、切片 ID 和分数。
4. 若命中案例，查询显式案例-法规关联，补充 `match_source`、`matched_alias`、`match_score` 和 `evidence_excerpt`，避免仅依赖语义推断。
5. 将“用户问题 + 已核验的证据片段 + 引用编号 + 不得虚构引用的约束”传入模型，或返回给界面/本地技能。
6. 输出必须区分：已检索事实、基于证据的分析、未知事实和需律师复核项。任何没有证据编号支持的具体案例、法条、段落或预测都应拒绝生成或标记为未证实。
7. 保存技能输入、过滤条件、证据和结果到 `skill_runs`，便于审计与回放；风险评估和研究备忘录默认 `review_required=true`。

当前分析服务已构造 `rag_context`，本地技能能输出检索/关联/证据核验结果。模型调用是否发生取决于 LLM 配置和分析模式；不能把“已构建 RAG 上下文”误解为“每次请求都由 LLM 自动生成法律意见”。

## 8. 如何提高效率

### 已采用

1. 只对缺失或正文哈希变化的切片编码，保证可恢复和幂等。
2. GPU 批处理、模型常驻和 BGE-M3 批量 `encode`，避免每条记录重复加载模型。
3. 对游戏场景可切换 CPU/GPU 工作器；切换发生在固定批次边界，检查点在数据库而非内存。
4. 先结构化过滤，再做候选检索；全文与向量只取有限候选，最终只传少量上下文给模型。
5. GIN 支持全文、B-tree 支持来源/model/结构化字段，HNSW 在向量全量完成后并发构建。
6. `EXPLAIN ANALYZE`、热身后的 P50/P95、模型冷启动与索引使用情况均可由基准脚本验证。

### 后续可实施，但需评测后采用

1. 对高频查询做短 TTL 缓存，键包含查询、过滤条件、模型版本和语料版本；数据更新时失效。
2. 增加查询改写：把中文问题、法规简称和法院缩写扩成英文同义术语，但保留原始查询用于审计。
3. 使用 BGE-M3 的多语言能力评测中英混合查询；若提升不足，不应增加复杂度。
4. 对 top-50 候选加入更慢的 cross-encoder reranker，仅重排少量候选，避免让全库检索退化成秒级以上。
5. 按 `source_kind` 或法域分区索引；只有当 `EXPLAIN` 显示过滤无法有效利用索引时再做，避免过度分区。
6. 在全量完成后以固定查询集扫描 `ef_search=40/80/120`，选择满足 Recall@12 目标下 P95 最低的值。

## 9. 如何提高检索质量与如何证明质量

不能以“相似度高”或“测试样本命中”直接声称 99% 准确。应建立三层评估：

| 层级 | 数据 | 指标 | 解释 |
| --- | --- | --- | --- |
| 数据质量 | 全库 | 空正文率、重复 UID、无效外键、向量维度、单位范数、未匹配引文率 | 保证输入和关系可用。 |
| 检索离线评估 | 人工标注问题集或高置信显式关联抽样 | Recall@K、MRR@K、nDCG@K、Precision@K | 衡量正确文档是否能排到前 K。 |
| 在线性能 | 固定查询集、冷热两种状态 | P50/P95/P99、错误率、索引计划 | 衡量真实响应时间与稳定性。 |

现有 `evaluate_case_retrieval.py` 从高置信案例-法规显式关联中固定随机抽样，使用法规标题检索案例，输出 `case_recall_at_K`、`case_mrr_at_K` 和延迟分位数。该评测能验证“现有关系集上的可检索性”，但不是人工法律正确率；需要另建按争点、法域、语言和时间分层的人工标注集。

建议验收门槛示例（需通过基线测试后由项目方确认）：全量向量维度为 1024、向量范数接近 1；显式关系必须能回指原文；冷热分开报告；HNSW Recall@12 相对精确扫描达到预设阈值；风险输出必须有足够数量的直接证据，否则下降为低置信并要求人工复核。

## 10. MCP 现状与应接入的工具

### 10.1 当前状态

当前**没有接入或运行 MCP 协议服务**，也没有把案例材料发送给第三方 MCP。已有的是 `skill_runtime_service.py` 本地技能注册表，包含案例检索、法规检索、争点事实提取、权威关联、证据核验、风险草稿和研究备忘录。这是一层本地编排与审计能力，不等同于 MCP Server。

### 10.2 推荐的 MCP 工具边界

若后续接入，应先提供只读、最小权限、输入输出可审计的本地工具，而不是直接允许模型执行任意 SQL、网络爬虫或文件命令：

| MCP 工具 | 输入 | 输出 | 价值与约束 |
| --- | --- | --- | --- |
| `search_cases` | 查询、法院/日期/语言、K | 案例切片、分数、引用 | 复用混合检索，强制参数化过滤和 K 上限。 |
| `search_statutes` | 查询、法域、生效时间、K | 法规切片、引文、URL | 防止案例/法规混淆，返回法律版本信息。 |
| `get_document_chunk` | 文档 ID、chunk_index | 原始片段与元数据 | 只读、长度上限，支持证据复核。 |
| `get_authority_links` | 案例或法规 ID | 显式关联、证据片段、匹配依据 | 展示可追溯关系，不把向量相似度伪装为引文。 |
| `verify_citations` | 引文列表 | 标准化、命中目标、未匹配项 | 让模型先核验再写作，降低幻觉。 |
| `retrieve_evidence_bundle` | 问题、过滤条件 | 去重后的 RAG 上下文和引用编号 | 统一给问答/备忘录使用的证据包。 |
| `evaluate_retrieval` | 评测集版本、K、索引配置 | Recall、MRR、nDCG、P95、失败样本 | 只允许离线评估，不允许任意生产写入。 |
| `ingestion_status` | 无或数据源 | 导入、切片、向量、索引进度 | 提供运行观测，不暴露密钥。 |

必须禁止或额外审批的工具：任意 SQL 执行、删除数据、无白名单网络抓取、上传案件全文到第三方、改变模型/索引参数、自动下结论的风险预测。MCP 接口应实行请求级白名单、速率限制、租户/用户隔离、提示注入检测、敏感字段脱敏、工具调用审计、超时和可关闭配置。

## 11. 关键风险与待完成项

1. 案例 BGE-M3 全量迁移尚在运行；完成前，未向量化的案例不会出现在正式 BGE-M3 语义检索中。
2. HNSW 目前按策略延后构建。全量完成后需并发建索引、验证索引有效性，再跑精确扫描对照与 `EXPLAIN ANALYZE`。
3. A2AJ 数据的许可、覆盖范围、更新频率和引用字段完整性必须按数据源许可证和版本持续记录；不能声称“加拿大全部法律”或“全部历史案例”。
4. 显式关联来自引文解析和别名匹配，仍可能有缩写歧义、版本歧义和未格式化引用。未匹配记录应保留并定期抽样人工复核。
5. 当前风险能力是“证据驱动的低/中置信研究草稿”，不是已训练并校准的裁判预测模型。若要训练，必须先定义标签、时间切分、防止数据泄漏、校准曲线、误差和人工复核流程。
6. 将来接入外部 API、商业数据库或 MCP 前，需要单独评估传输数据的保密性、许可、成本、可用性和审计要求。

## 12. 一次查询的最小示例

用户提问：“Ontario 雇佣关系中因投诉被解雇的风险？”

1. 解析得到 `jurisdiction=Ontario`、主题 `employment/dismissal/complaint`，若用户提供时间则加日期过滤。
2. 案例分区和法规分区各自运行全文与 BGE-M3 检索，返回有限候选。
3. 用 RRF 融合，过滤无关键词支撑的弱纯向量结果，再本地重排。
4. 对前排案例读取显式法规关系，例如关联到 Employment Standards Act 的记录必须带原始命中片段和关系分数。
5. 构造包含案例片段、法条片段、URL、`R1...Rn` 的证据包。
6. 回答只陈述证据直接支持的规则与事实缺口；没有已验证案例或法条时回答“证据不足”，而不是编造结论。

## 13. 推荐运行与验证顺序

1. 让 GPU/CPU 工作者完成所有缺失 BGE-M3 向量；检查每个来源的总数、已完成数、待处理数和 `percentage`。
2. 验证向量维度、模型/provider、空值、重复 chunk、内容哈希和单位范数。
3. 在维护窗口用 `CREATE INDEX CONCURRENTLY` 构建 HNSW，检查 `indisvalid=true`，不得留下无效索引。
4. 使用 `scripts/benchmark_retrieval.py --explain` 测试词法、向量和混合检索的热身后 P50/P95，并确认查询计划实际使用合适索引。
5. 使用 `scripts/evaluate_case_retrieval.py` 记录关系抽样的 Recall@K、MRR、失败样本；再引入人工标注集做独立评测。
6. 仅在上述结果和许可检查通过后，开放给问答、风险草稿或 MCP 工具；每次输出仍保留证据、分数、模型版本和人工复核标记。

